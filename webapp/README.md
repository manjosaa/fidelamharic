Responsive Webapp (React + Vite)

Quick start:

1. cd webapp
2. npm install
3. npm run dev

This is a static frontend scaffold intended for desktop and mobile (responsive). Build with `npm run build` and serve the `dist/` folder.
